#import pygame


#def mostrar_pausa(pantalla, fondo, reloj):
#   en_pausa = True
  #  while en_pausa:
   #     for evento in pygame.event.get():
    #        if evento.type == pygame.QUIT:
      #          pygame.quit()
       #         quit()
        #    if evento.type == pygame.KEYDOWN:
         #       if evento.key == pygame.K_SPACE:
          ##          en_pausa = False

#        pantalla.blit(fondo, (0, 0))
#       fuente = fuente.render("pausa", True, (255, 255, 255))
#      texto = fuente.render("Pausa", True, (255, 255, 255))
#        pantalla.blit(texto, (pantalla.get_width() // 2 - texto.get_width() //
#                     2, pantalla.get_height() // 2 - texto.get_height() // 2))
#      pygame.display.flip()
#      reloj.tick(120)
